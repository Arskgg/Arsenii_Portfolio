import styles from "./Contact.module.scss";
import SectoionContainer from "../SectionContainer";
const Contact = () => {
  return (
    <section className={styles.contact}>
      <SectoionContainer title="Contact"></SectoionContainer>
    </section>
  );
};

export default Contact;
